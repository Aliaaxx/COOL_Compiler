/*
package main.java.compiler.test;

import main.java.compiler.semantics.COOLBaseListener;
import main.generated.main.antlr.COOLLexer;
import main.generated.main.antlr.COOLParser;
import org.antlr.v4.gui.Trees;
import org.antlr.v4.runtime.*;
import org.antlr.v4.runtime.tree.*;
//import compiler.generated.*;

public class ParserTest {
    public static void main(String[] args) throws Exception {
        /*
        CharStream input = CharStreams.fromFileName("src/main/resources/testLexer.cl");
        //COOLLexer lexer = new COOLLexer(CharStreams.fromString(input));
        //CommonTokenStream tokens = new CommonTokenStream(lexer);
        COOLParser parser = new COOLParser(tokens);

        ParseTree tree = parser.program(); // Start rule
        System.out.println(tree.toStringTree(parser));

        // Parse input and get the parse tree
        // ParseTree Ptree = parser.expr();

        // Display parse tree GUI
        // Trees.inspect(Ptree, parser);



        // Create and walk the listener
        // COOLBaseListener listener = new COOLBaseListener();
        // ParseTreeWalker walker = new ParseTreeWalker();
        // walker.walk((ParseTreeListener) listener, tree);

        // ParseTreeListener listener = new COOLBaseListener(); // Use interface type

        // ParseTreeWalker walker = new ParseTreeWalker();
        // walker.walk(listener, tree);
        // COOLBaseListener listener = new COOLBaseListener();
        // ParseTreeWalker walker = new ParseTreeWalker();
        // walker.walk(listener, tree);


    }
}
*/